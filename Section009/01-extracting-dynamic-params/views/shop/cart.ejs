<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
                <% if(products.length > 0) { %> console.log(p);
                    <ul>
                        <% products.forEach(p => { %>
                            <li>
                                <p><%= p.productdata.title %> ( <%= p.qty %> ) </p>
                            </li>
                        <% }) %>

                    </ul>
                <% } else { %>
                    <h1>No products in the cart. </h1>
                <% } %>
        </main>
        <%- include('../includes/end.ejs') %>